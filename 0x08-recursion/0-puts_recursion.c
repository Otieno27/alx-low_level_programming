#include "main.h"

/**
 * puts_recursion - prints a string
 * @s: string to be printed
 */
void _puts_recursion(char *s)
{
	if (*)
	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}

	else
		_putchar('\n');
}
